name: Stock Bot Scheduler

on:
  workflow_dispatch:

jobs:
  test-run:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run test
        run: echo "Workflow working"
